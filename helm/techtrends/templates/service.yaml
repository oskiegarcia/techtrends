apiVersion: v1
kind: Service
metadata:
  name: techtrends-svc
  labels:
    app: techtrends
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: techtrends
